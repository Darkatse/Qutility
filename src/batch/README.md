# batch 模块

提供统一的批量文件处理能力，支持并行计算。

## 架构位置

作为通用基础设施层，被 `commands/` 模块调用实现批量处理功能。

## 模块结构

| 文件 | 功能 |
|------|------|
| `collector.rs` | 文件收集器：glob 匹配、递归搜索 |
| `runner.rs` | 并行执行器：rayon 调度、进度条、统计 |
